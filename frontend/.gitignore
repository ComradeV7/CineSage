# Dependencies
# This folder contains all the downloaded packages and can be very large.
# It should always be ignored and regenerated by running 'npm install' or 'yarn install'.
/node_modules

# Build output
# These folders contain the final, optimized code that you deploy.
# They are generated by running 'npm run build' or 'yarn build'.
/build
/dist
/.output
/.next

# Local environment variables
# These files contain sensitive information like API keys and should never be committed to Git.
# A .env.example file should be committed instead to show what variables are needed.
.env
.env.local
.env.development.local
.env.production.local
.env.test.local

# Logs
# Log files generated during development or testing.
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
# These are temporary files created by code editors like VSCode.
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS generated files
.DS_Store
Thumbs.db